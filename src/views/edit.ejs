<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/addproduct.css" />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include('include/_header', {name: 'Admin', header: 'Sửa sản phẩm'}) %>
    <main class="management">
      <div class="form">
        <form method="post">
          <h3 class="form-heading">Sửa sản phẩm:</h3>
          <div class="form-content">
            <div class="form-content__list">
              <label for="nameSP" class="form-content__child"
                >Tên sản phẩm:</label
              >
              <input
                type="text"
                id="nameSP"
                class="form-content__field"
                name="name"
                value="<%= data.name %>"
              />
            </div>

            <div class="form-content__list">
              <h3 class="form-content__child">Đơn giá:</h3>
              <input
                type="number"
                class="form-content__field"
                name="price"
                value="<%= data.price %>"
              />
            </div>
            <div class="form-content__list">
              <h3 class="form-content__child">Mô tả sản phẩm:</h3>
              <textarea name="desc" id="" cols="80" rows="5">
<%= data.desc %></textarea
              >
            </div>

            <div class="form-content__list">
              <h3 class="form-content__child">Ảnh sản phẩm:</h3>
              <!-- <label for="form-file" class="form-select">Chọn tệp</label>
              <input
                type="file"
                id="form-file"
                class="form-content__field"
                name="picture"
                placeholder="Thêm tệp"
                style="display: none"
              /> -->
              <input
                type="text"
                class="form-content__field"
                name="picture"
                value="<%= data.picture %>"
              />
            </div>

            <div class="form-content__list">
              <h3 class="form-content__child">Số lượng sản phẩm:</h3>
              <input
                type="number"
                class="form-content__field"
                name="amount"
                value="<%= data.amount %>"
              />
            </div>
            <div class="form-content__list">
              <h3 class="form-content__child">Đã bán:</h3>
              <input
                type="number"
                class="form-content__field"
                name="sold"
                value="<%= data.sold %>"
              />
            </div>

            <div class="form-content__list">
              <h3 class="form-content__child">Loại sản phẩm:</h3>
              <input
                type="text"
                class="form-content__field"
                name="type"
                value="<%= data.type %>"
              />
            </div>
          </div>
          <button type="submit" class="btn-add__product">Chỉnh sửa</button>
        </form>
      </div>
      <%- include('include/_dashboard') %>
    </main>
  </body>
</html>
